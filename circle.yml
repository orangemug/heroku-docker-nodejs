machine:
  services:
    - docker

dependencies:
  pre:
    - pip install docker-compose
  override:
    - docker-compose build

test:
  override:
    - docker-compose run --service-ports web npm test
