machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install docker-compose
  override:
    - docker-compose build

test:
  override:
    - docker-compose run --service-ports web npm test
